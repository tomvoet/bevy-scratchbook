[package]
name = "sims"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { workspace = true, features = ["ui", "bevy_feathers", "ui_picking", "mesh_picking"] }
fluid-2d = { path = "../crates/fluid-2d" }

[features]
dev = ["bevy/dynamic_linking"]
